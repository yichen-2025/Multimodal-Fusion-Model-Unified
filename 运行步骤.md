# 多模态融合模型运行步骤

## 项目简介

本项目实现了一个基于多模态融合的网络流量分类模型，结合统计特征模态和文本描述模态进行恶意流量检测。

### 主键体系

项目采用三级主键体系管理数据和模型：

| 主键 | 作用 | 示例 |
|------|------|------|
| `dataset_id` | 标识数据集子集 | `dataset_0.csv`, `dataset_1.csv` |
| `split_id` | 标识同一数据集的不同划分 | `split_data/dataset_0/split_0/` |
| `model_id` | 标识不同的训练结果 | `saved_models/model_0/` |

---

## 运行方式

项目通过 `main.py` 文件统一管理所有运行步骤，使用方法如下：

1. 打开 `main.py` 文件
2. 取消注释对应步骤的代码块（一次只取消一个步骤）
3. 运行命令：`python main.py`
4. 步骤完成后，注释该步骤代码，继续下一步

---

## 步骤详解

### 步骤0：安装依赖

```bash
pip install torch transformers datasets scikit-learn pandas numpy pytest matplotlib
```

> 默认已完成，若缺少依赖可执行上述命令。

### 步骤0：下载预训练模型

```bash
python scripts/download_bert.py
python scripts/download_qwen.py
```

- **涉及文件**：`scripts/download_bert.py`, `scripts/download_qwen.py`
- **模型路径**：`./models/bert/`, `./models/qwen2.5-1.5b/`

---

### 步骤1：数据清洗与预处理

**操作**：取消 `main.py` 中步骤1的注释

```python
# ============================================
# 步骤1：数据清洗
# ============================================
from scripts.data_cleaning import main as run_data_cleaning
run_data_cleaning(dataset_filename="IoT Network Intrusion Dataset.csv")
```

**运行**：`python main.py`

**参数说明**：
- `dataset_filename`（可选）：指定数据集文件名，不指定则自动使用第一个CSV文件

**输入**：`data_processing/` 目录下的CSV文件

**输出**：`processed_dataset/processed_dataset.csv`

**处理内容**：缺失值处理、异常值检测、标签编码（Normal→0, Anomaly→1）

**命令行直接运行**：
```bash
python scripts/data_cleaning.py --dataset "IoT Network Intrusion Dataset.csv"
```

---

### 步骤2：提取数据集子集

**操作**：取消 `main.py` 中步骤2的注释

```python
# ============================================
# 步骤2：提取数据集子集
# ============================================
success, dataset_id = extract_subset(
    num_samples=5000,
    # dataset_id=0,  # 可选：指定数据集ID
    random_state=42
)
if success:
    print(f"成功提取子集，数据集ID: {dataset_id}")
else:
    print("提取子集失败")
```

**运行**：`python main.py`

**参数说明**：
- `num_samples`: 提取样本数量（默认5000）
- `dataset_id`: 数据集ID（默认自动递增）
- `random_state`: 随机种子（默认42）

**输出**：
- `processed_dataset/dataset_X.csv`（子集数据）
- `processed_dataset/subset_X_scaled_features.npy`（标准化特征）
- `processed_dataset/subset_X_labels.npy`（标签）
- `processed_dataset/subset_X_scaler.npy`（标准化器参数）

**日志记录**：自动生成 `logs/subset/log_X.json`

---

### 步骤3：模态分离与数据集划分

**操作**：取消 `main.py` 中步骤3的注释，确保 `dataset_id` 与步骤2一致

```python
# ============================================
# 步骤3：模态分离与数据集划分
# ============================================
dataset_id = 0  # 与步骤2的dataset_id一致
split_modality(
    dataset_id=dataset_id,
    # split_id=0,  # 可选：指定划分ID
    test_size=0.2,
    random_state=42
)
print(f"成功完成模态分离，数据集ID: {dataset_id}")
```

**运行**：`python main.py`

**参数说明**：
- `dataset_id`: 数据集ID（默认0）
- `split_id`: 划分ID（默认自动递增）
- `test_size`: 测试集比例（默认0.2）
- `random_state`: 随机种子（默认42）

**输出**：`split_data/dataset_X/split_Y/`
- `train.npz`（训练集：统计特征 + BERT嵌入 + 标签）
- `test.npz`（测试集：统计特征 + BERT嵌入 + 标签）
- `train_data.csv`（训练集原始数据）
- `test_data.csv`（测试集原始数据）
- `train_scaler.npy`（训练集标准化器参数）

**日志记录**：自动生成 `logs/split/log_X.json`

---

### 步骤4：模型训练

**操作**：取消 `main.py` 中步骤4的注释，确保 `dataset_id` 和 `split_id` 与步骤3一致

```python
# ============================================
# 步骤4：模型训练
# ============================================
try:
    train_model(
        model_path="./models/qwen2.5-1.5b",
        # model_id=0,  # 可选：指定模型ID
        dataset_id=0,
        split_id=0,
        per_device_train_batch_size=2,
        gradient_accumulation_steps=4,
        learning_rate=1e-4,
        num_train_epochs=3
    )
    print("训练完成")
except Exception as e:
    print(f"训练出错：{e}")
```

**运行**：`python main.py`

**参数说明**：
- `model_path`: LLM模型路径（默认`./models/qwen2.5-1.5b`）
- `model_id`: 模型ID（默认自动递增）
- `dataset_id`: 数据集ID（默认0）
- `split_id`: 划分ID（默认0）
- `per_device_train_batch_size`: 每个设备的batch大小（默认2）
- `gradient_accumulation_steps`: 梯度累积步数（默认4）
- `learning_rate`: 学习率（默认1e-4）
- `num_train_epochs`: 训练轮数（默认3）

**输出**：`saved_models/model_X/`
- `pytorch_model.bin`（模型参数）
- `config.txt`（训练配置）
- `loss_logs/loss_log.csv`（训练loss记录）
- `loss_logs/loss_log.json`（loss记录JSON格式）

**日志记录**：自动生成 `logs/training/log_X.json`

---

### 步骤5：模型测试

**操作**：取消 `main.py` 中步骤5的注释，确保 `model_id`、`dataset_id`、`split_id` 与之前步骤一致

```python
# ============================================
# 步骤5：模型测试
# ============================================
try:
    result = test_model(
        model_id=0,
        dataset_id=0,
        split_id=0,
        verbose=True
    )
    print("\n测试结果：")
    print(f"准确率: {result['accuracy']:.4f}")
    print(f"精确率: {result['precision']:.4f}")
    print(f"召回率: {result['recall']:.4f}")
    print(f"F1值: {result['f1']:.4f}")
except Exception as e:
    print(f"测试出错：{e}")
```

**运行**：`python main.py`

**参数说明**：
- `model_id`: 模型ID（与`--saved_model_path`二选一）
- `dataset_id`: 数据集ID（默认0）
- `split_id`: 划分ID（默认0）
- `verbose`: 是否打印详细信息（默认True）

**输出**：
- 控制台打印评估指标（准确率、精确率、召回率、F1值）
- `test_reports/report_X.json`（测试报告JSON）
- `test_reports/reports_index.csv`（报告索引CSV）

---

### 步骤6：绘制loss曲线

**操作**：取消 `main.py` 中步骤6的注释

```python
# ============================================
# 步骤6：绘制loss曲线（训练完成后执行）
# ============================================
plot_loss_curve(model_id=0)
```

**运行**：`python main.py`

**参数**：`model_id`（训练模型的ID）

**效果**：弹出窗口显示训练loss变化曲线

---

## 完整流程示例

打开 `main.py`，按顺序取消注释执行：

```python
# 第1次运行：数据清洗
from scripts.data_cleaning import main as run_data_cleaning
run_data_cleaning(dataset_filename="IoT Network Intrusion Dataset.csv")

# 第2次运行：提取子集
success, dataset_id = extract_subset(num_samples=5000, random_state=42)

# 第3次运行：模态分离
dataset_id = 0
split_modality(dataset_id=dataset_id, test_size=0.2, random_state=42)

# 第4次运行：模型训练
train_model(model_path="./models/qwen2.5-1.5b", dataset_id=0, split_id=0, num_train_epochs=3)

# 第5次运行：模型测试
result = test_model(model_id=0, dataset_id=0, split_id=0, verbose=True)

# 第6次运行：绘制loss曲线
plot_loss_curve(model_id=0)
```

---

## 注意事项

### 硬件要求

- **GPU**：推荐 NVIDIA GPU（Ampere架构及以上），支持bfloat16混合精度训练
- **显存**：至少16GB（训练Qwen2.5-1.5B模型）
- **内存**：至少16GB（处理大数据集）

### 数据格式

- 原始数据集：CSV格式，包含`Label`列（值为`Normal`或`Anomaly`）
- 支持的特征列：`Flow_Duration`, `Tot_Fwd_Pkts`, `Tot_Bwd_Pkts`, `TotLen_Fwd_Pkts`, `TotLen_Bwd_Pkts`, `Flow_Byts/s`, `Fwd_Pkt_Len_Mean`, `Bwd_Pkt_Len_Mean`, `Pkt_Len_Mean`

### GPU检查机制

所有需要GPU的程序在执行前会自动检查GPU可用性。若未检测到GPU，会提示用户是否继续使用CPU运行。

### 日志系统

所有操作都会自动生成日志文件：

| 日志类型 | 保存位置 | 内容 |
|---------|---------|------|
| 子集提取 | `logs/subset/log_X.json` | 时间、数据集ID、样本数、正负样本数 |
| 数据划分 | `logs/split/log_X.json` | 时间、数据集ID、划分ID、训练/测试样本数 |
| 模型训练 | `logs/training/log_X.json` | 时间、模型ID、数据集ID、训练参数、耗时 |
| 模型测试 | `test_reports/report_X.json` | 时间、模型ID、评估指标、混淆矩阵 |

### 测试脚本

运行项目测试套件验证所有功能：

```bash
python -m pytest scripts/test_project.py -v
```

---

## 文件结构

```
多模态融合3/
├── main.py                    # 主入口文件（推荐运行方式）
├── scripts/                   # 脚本模块
│   ├── data_cleaning.py       # 数据清洗
│   ├── extract_subset.py      # 子集提取
│   ├── split_modality.py      # 模态分离
│   ├── train.py               # 模型训练
│   ├── test_model.py          # 模型测试
│   ├── test_project.py        # 项目测试
│   ├── download_bert.py       # BERT下载
│   └── download_qwen.py       # Qwen下载
│
├── data_processing/           # 原始数据
│   └── *.csv
│
├── processed_dataset/         # 处理后数据
│   ├── processed_dataset.csv
│   └── dataset_X.csv
│
├── split_data/                # 划分后数据
│   └── dataset_X/split_Y/
│       ├── train.npz
│       ├── test.npz
│       └── ...
│
├── saved_models/              # 训练模型
│   └── model_X/
│       ├── pytorch_model.bin
│       ├── config.txt
│       └── loss_logs/
│
├── models/                    # 预训练模型
│   ├── bert/
│   └── qwen2.5-1.5b/
│
├── logs/                      # 操作日志
│   ├── subset/
│   ├── split/
│   └── training/
│
├── test_reports/              # 测试报告
│   ├── reports_index.csv
│   └── report_X.json
│
├── src/                       # 核心源码
│   ├── data/
│   │   └── data_loader.py
│   └── model_architectures/
│       ├── bert_encoder.py
│       ├── numeric_encoder.py
│       ├── fusion_projection.py
│       └── multi_modal_model.py
│
├── utils/                     # 工具模块
│   └── log_utils.py
│
└── docs/                      # 文档
    ├── theory.md
    └── IoT_Dataset_Migration.md
```